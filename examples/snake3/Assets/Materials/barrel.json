{
  "_comment": "Barrel textures (albedo, normalMap) are intentionally omitted: BarrelNode3D::update polls feature->getAlbedo()==nullptr and assigns the async-loaded model texture lazily. Feature order is NOT load-bearing - shader cache key is built from a feature bitmask (ShaderRegistry::makeKey), so reordering is safe.",
  "master": "basicShader",
  "blending": "opaque",
  "features": [
    { "type": "lighting" },
    { "type": "shadow" },
    { "type": "normalMap" },
    { "type": "albedo", "ambientIntensity": 2.5 },
    { "type": "fog" }
  ]
}
